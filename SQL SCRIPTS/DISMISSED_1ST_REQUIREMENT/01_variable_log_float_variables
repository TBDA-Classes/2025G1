-- get the list of variables that have float logs
SELECT
    v.id,
    v.name
FROM
    public.variable AS v
WHERE
    v.id IN (SELECT DISTINCT id_var FROM public.variable_log_float)
ORDER BY
    v.name;
