SELECT
    alarm_code,
    alarm_text,
    COUNT(*) AS nb_occurrences,
    SUM(duration) AS total_duration
FROM (
--insert the previous code here inside the () 
) AS t
GROUP BY alarm_code, alarm_text
ORDER BY total_duration DESC;
