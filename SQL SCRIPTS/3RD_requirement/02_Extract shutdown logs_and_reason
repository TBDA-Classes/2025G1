--We will now extract all the logs associated with these two variables, sorted in chronological order.
--This will then allow us to analyze the sequences of shutdowns and their causes.

SELECT id_var,
       to_timestamp(date / 1000) AS log_time,
       value
FROM variable_log_string
WHERE id_var IN (447, 557)
  AND value IS NOT NULL
  AND value <> '[]'
ORDER BY date ASC
LIMIT 200;


--we see mentions like “EMERGENCIA EXTERNA”, “FALLO PRESIÓN GENERAL DE AIRE”, “PARADA DE AVANCES”, etc.
